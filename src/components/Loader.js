import React from "react";
import "./Loader.css"; // 确保加载 CSS 文件

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="loader"></div>
    </div>
  );
};

export default Loader;